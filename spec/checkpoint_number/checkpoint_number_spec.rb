require File.expand_path(File.dirname(__FILE__) + "/../spec_helper")
require "team_entry"

describe "Checkpoint Number", "save" do

  uses_limelight :scene => "checkpoint_number", :hidden => true
  #   
  # it "has the checkpoint number update button" do
  #   checkpoint_button = scene.find("checkpoint_button")
  #   checkpoint_button.should_not be_nil    
  # end
  # 
  # it "saves the checkpoint number and redirects to the main scene" do
  #   checkpoint_number = scene.find("checkpoint_number")
  #   checkpoint_number.text = "2"
  # 
  #   checkpoint_button = scene.find("checkpoint_button")
  #   checkpoint_button.button_pressed(nil)
  #   
  #   CONFIG[:checkpoint].should == "2"
  # end
  
  
end
